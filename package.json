{
  "name": "mean-demo",
  "version": "0.0.1",
  "scripts": {
    "start": "node ${SCRIPT:-app.js}",
    "deploy": "gcloud preview app deploy app.yaml worker.yaml"
  },
  "dependencies": {
    "express": "^4.3.1",
    "body-parser": "^1.2.2",
    "mongoose": "^4.x.x"
  }
}
